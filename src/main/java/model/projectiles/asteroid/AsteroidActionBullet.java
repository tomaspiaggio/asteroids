package model.projectiles.asteroid;

import model.interfaces.Model;
import model.projectiles.bullet.Bullet;
import util.Action;
import util.Option;

/**
 * Created by Tomas on 10/21/17.
 */
public class AsteroidActionBullet implements Action {

    @Override
    public void performAction(Option... options) {
//        final Option<Asteroid> asteroid = options[0];
//        final Option<Model> bullet = options[0];
//        ((Bullet) bullet.getValue()).decrementDamage(asteroid.getValue().getLife());
    }

}
